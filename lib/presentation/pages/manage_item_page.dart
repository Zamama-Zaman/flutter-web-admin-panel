import 'package:flutter/material.dart';

class ManageItemPage extends StatelessWidget {
  static const String id = "manage-item-page";

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Manage Item"),
    );
  }
}
