import 'package:flutter/material.dart';

class ManageCategoryPage extends StatelessWidget {
  static const String id = "manage-category-page";

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Manage Category Page"),
    );
  }
}
